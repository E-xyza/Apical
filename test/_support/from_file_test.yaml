openapi: 3.1.0
info:
  title: FromFileTest
  version: 1.0.0
paths:
  "/{number}":
    get:
      operationId: route
      parameters:
        - name: number
          in: path
          required: true
          schema:
            type: integer
            minimum: 0
      responses:
        "200":
          description: OK